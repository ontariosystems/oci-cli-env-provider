name: test
description: tests oci-cli-env-provider
inputs:
  go-version:
    description: "the version of golang"
    default: '1.25'
runs:
  using: composite
  steps:
    - uses: actions/setup-go@v5
      with:
        go-version: ${{ inputs.go-version }}
    - run: make test
      shell: bash
